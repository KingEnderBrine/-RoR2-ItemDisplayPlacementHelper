**1.0.3**

* Added the ability to specify `childPath` with a relative path to a `GameObject` with `ChildLocator`, that will be added to the `ChildLocator`.

**1.0.2**

* Fixed an issue where idrs with missing keyAsset would prevent game loading.

**1.0.1**

* Added support for skin-specific idrs. If `.idrsJson` has not empty `skinName` field, the rules will be applied only to that skin.

**1.0.0**

* Mod release.